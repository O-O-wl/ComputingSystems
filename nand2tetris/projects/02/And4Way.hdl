/* And4Way */
CHIP And4Way {
    IN in[4];
    OUT out;

    PARTS:
    // Put your code here:
    And(a=in[0], b=in[1], out=and1);
    And(a=and1, b=in[2], out=and2);
    And(a=and2, b=in[3], out=out);
}